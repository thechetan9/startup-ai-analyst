# Environment Variables for AI Startup Analyst Frontend

# Backend API URL - will be set automatically during deployment
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Enable/disable backend features
NEXT_PUBLIC_ENABLE_BACKEND=true

# Firebase Configuration (automatically loaded from Firebase SDK)
# These are set in firebase config, no need to duplicate here

# Development vs Production
NODE_ENV=development
